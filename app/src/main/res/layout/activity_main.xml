<?xml version="1.0" encoding="utf-8"?>
<ScrollView  xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16.dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginVertical="16.dp"
            android:text="ИИ Генератор картинокь от Атема)) 🤖"
            android:textColor="#3E2723"
            android:textSize="22sp"
            android:textStyle="bold" />

        <EditText
            android:id="@+id/etInputText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxHeight="120dp"
            android:background="@android:drawable/edit_text"
            android:gravity="top|start"
            android:hint="Вставте текст"
            android:importantForAutofill="no"
            android:inputType="textMultiLine"
            android:padding="12.dp" />

        <Button
            android:id="@+id/btnParse"
            android:layout_width="match_parent"
            android:layout_height="55.dp"
            android:layout_marginTop="16.dp"
            android:backgroundTint="#5D4037"
            android:text="РАСПРЕДЕЛИТЬ ПО ТАБЛИЦЕ"
            android:textColor="#FFFFFF"

            android:textSize="16sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="8dp">
        <!-- Новая кнопка Очистить -->
        <Button
            android:id="@+id/btnClear"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:backgroundTint="#B0BEC5"
            android:text="ОЧИСТИТЬ ВСЁ"
            android:textColor="#37474F" />

        <!-- Кнопка генерации изображения -->
        <Button
            android:id="@+id/btnGenerateImg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Сгенерировать картинку" />

    </LinearLayout>

        <Button
            android:id="@+id/btnSaveImage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Сохранить картинку ИИ"
            android:visibility="gone" /> <!-- По умолчанию скрыта, появится когда картинка дожарится -->


        <!-- ImageView для вывода картинки -->
        <ImageView
            android:id="@+id/ivGeneratedResult"
            android:layout_width="300dp"
            android:layout_height="300dp"
            android:layout_gravity="center"
            android:visibility="gone"
            android:layout_marginTop="16dp" />


        <LinearLayout
            android:id="@+id/layoutResult"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24.dp"
            android:background="#FFFFFF"
            android:orientation="vertical"
            android:padding="16.dp"
            android:visibility="gone"
            tools:visibility="visible">

            <TextView
                android:id="@+id/tvResult"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="monospace"
                android:lineSpacingMultiplier="1.2"
                android:text="Тут будет результат"
                android:textColor="#212121"
                android:textSize="13sp" />

            <!-- Кнопка 1: Копирование в буфер -->
            <Button
                android:id="@+id/btnCopy"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16.dp"
                android:backgroundTint="#8D6E63"
                android:text="СКОПИРОВАТЬ В БУФЕР"
                android:textColor="#FFFFFF" />

            <!-- Кнопка 2: Сохранение в файл TXT -->
            <Button
                android:id="@+id/btnSaveFile"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8.dp"
                android:backgroundTint="#4E342E"
                android:text="СКАЧАТЬ ТЕКСТОВЫЙ ФАЙЛ"
                android:textColor="#FFFFFF" />
        </LinearLayout>

    </LinearLayout>
</ScrollView>